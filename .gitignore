# 明文密钥 / 本机路径
box_config.json
box_config.json.bak
box_config.json.*
*.bak

# 运行产物（用户数据，不入 git）
source/
cropped_hi/
output/
token_log.csv
launcher.log
*.tmp

# Python
__pycache__/
*.pyc

# 构建中间产物
build/
dist/
build_venv*/
macOS/AppIcon.icns
.portable_staging

# Release 产物（二进制走 GitHub Releases，不进 git 仓库）
deploy/Output/

# 通用日志
*.log
build_log.txt
iss_log.txt

# 旧版 / 调试残留（scripts/，非核心代码）
scripts/box_ocr_server.py
scripts/call_ocr.py
scripts/group_articles.py
scripts/test_verify.py
scripts/static/
scripts/民国报纸OCR/
scripts/box_launcher.py.current_img_*
# 注：scripts/stop_flag.py 是核心运行时文件（子进程 --run-script 机制依赖，
#     box_launcher.py 会 import 它），必须随仓库发布，故不在此忽略。

# 开发者私有文件（本人内部文档，不对外公开、不进任何远程仓库）
SKILL.md
