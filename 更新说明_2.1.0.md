# 墨痕 · 近代报刊转录助手 2.1.0 更新说明

- **版本号**：2.1.0
- **基础版本**：2.0.5（2.0.6 未正式发布，本次合并其改动并修正）

## 修复（Bug Fixes）

1. **修复结构化（题录）完成后误删其它页面源图**
   单页 / 跨页清理时只删除本次已结构化处理的整版，不再误删尚未识别版抽出的 `cropped_hi` 源图。

2. **修复跨页模式翻页时校对文本框被重置**
   翻页不再重建结果面板，保留已填写的文本框内容；并重建快照高度与滚动位置，翻回原页时文本框不被清空。

3. **修复框选误触**
   拖拽控制点（角 / 边）时只选中当前框，不再误触发整框移动；排版紧凑或框体重叠时，按住 **Alt + 左键** 可从空白处强制画新框。

4. **修复海外用户自动更新下载失败**
   更新源新增 **Gitee ↔ GitHub 自动回退**：双查最新版本号 + 主备下载循环，任一源不可达时自动切换，提升跨国更新成功率。

## 改进（Improvements）

1. **PDF 抽图性能优化**
   去白边改在低分辨率副本上求内容边界框、移除 PDF 冗余优化步骤，实测抽图耗时显著降低（约 60%）。

2. **防止重复触发抽图**
   新增模块级锁（`_extract_lock` / `_extract_busy`）+ 前端按钮禁用双保险，避免连续点击导致并发抽图或界面卡死。

## 升级方式

- **自动更新（已开启）**：检测到 2.1.0 后自动下载并重启（Gitee / GitHub 双源）。
- **手动**：运行 `build_exe.bat` 完整重打包，或用新版 exe 覆盖安装。
- **数据目录不变**：识别产物仍落在「文档 / 墨痕数据」，跨版本共享，无需迁移。
