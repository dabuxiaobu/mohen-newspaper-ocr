# 墨痕 3.0.0 更新说明

发布日期：2026-09-22
版本定位：继 2.1.0 之后的重大功能版本。3.0.0 聚焦「转录即出规范题录」与「多图连续识别体验」两大方向，集中回应 140 人用户群的真实反馈（双格式题录、《历史研究》页下注、繁简转换、多图识别连坐、缩微 PDF 抽图等）。

---

## 一、新增功能

### 1. 双格式题录（GB/T 7714-2015 + 《历史研究》页下注）
- 转录完成后，在结果面板内一键切换 **GB/T 7714-2015** 与 **《历史研究》页下注** 两种引文格式；
- 手动修改作者、日期、引用串后，**即时本地重算**，无需重新识别；
- 报纸 `[N]` 与期刊 `[J]` 两类文献均支持。

### 2. 繁简转换 + 断行合并
- 结构化后自动经 OpenCC（t2s）转为简体，并按句末标点智能断行；
- 修复此前「结构化后仍是繁体、无断句」的问题。

### 3. 识别子集化 + 连坐防护
- 单页 / 跨页模式载入多张图后，可只框选并识别其中若干张；**未识别的已载入图不再被连坐清空**；
- 支持连续逐张识别，解决「载入 5 张只识别 1 张、其余消失无法连续识别」的问题。

### 4. PDF 抽图 pdfium 本地渲染回退
- 默认解析失败（如缩微档案扫描件 PDF）时，自动切换 **pdfium 本地渲染** 抽图；
- 解决部分 PDF 抽图后无反应、无法生成图片的问题。

### 5. 期刊 `[J]` 引用本地重算联动
- 覆盖报纸与期刊两类文献，引用格式随文献类型在本地即时重算。

### 6. 交互提示与 UI 优化
- 保存修改后显示「已保存 ✓」，识别完成显示「识别完成 ✓」；
- 粘贴截图载入区 UI 优化，避免误认「没反应」；
- 框选 / 平移切换、护眼模式、来源补充等细节打磨。

---

## 二、修复

- 繁体未转简 / 无断句；
- 多图连坐清空导致无法连续识别；
- 缩微档案 PDF 抽图失效；
- 子进程目录对齐、结构化命名等历史遗留问题。

---

## 三、更新方式

- 应用内「检查更新」自动拉取（Windows / macOS 均支持）；
- 或前往 GitHub / Gitee 的 `mohen-newspaper-ocr` release 手动下载安装包。

---

## 四、已知范围

- 自动更新采用严格字符串版本比较，`3.0.0` 高于 `2.1.0`，旧用户会收到更新提示；
- macOS 包由 GitHub Actions 自动构建（arm64），Gitee 端 release 会同步补传 Mac 包。
