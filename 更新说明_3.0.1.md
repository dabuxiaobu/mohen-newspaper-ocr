# 墨痕 3.0.1 更新说明

发布日期：2026-09-23
版本定位：3.0.0 之后的增量功能版本，聚焦「整版原图管理」与「扫描件方向校正」两项高频体验改进。

---

## 一、新增功能

### 1. 整版原图排序抽屉
- 整版列表头部新增「排序」按钮，打开抽屉可依据 **默认顺序 / 文件名 / 修改时间** × **升 / 降序** 一键重排；
- 排序结果持久化到 `cropped_hi/.order.json`，对「载入」与翻页顺序一并生效，且跨会话保留。

### 2. 整版原图拖拽排序
- 整版列表项支持鼠标 **拖拽** 调整顺序，与原有 ↑/↓ 箭头并存；松手即重排并自动持久化。

### 3. 图片旋转
- 画布缩放栏的「适应窗口」按钮改为 **⟳ 顺时针旋转 90°**（连点 4 次回原位）；
- 旋转为 **物理旋转** `cropped_hi/` 中的整版图（PNG 无损），框选坐标、OCR 裁剪、抽图全部基于旋转后的图，不会错位；
- 旋转会清空当前版已画框（坐标失效），并重载显示；
- 「适应窗口」入口保留：双击缩放百分比（100%）即可恢复适应窗口。

---

## 二、性能与稳定性
- 旋转改为 C 层 `transpose` 直换像素 + 低压缩级别保存，且旋转显示即时本地完成、保存后台持久化，告别此前整版大图回传导致的数秒卡顿。
- 自动更新采用严格字符串版本比较，`3.0.1` 高于 `3.0.0`，旧用户会收到更新提示。
